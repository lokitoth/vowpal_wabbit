resources:
- repo: self

pool:
  name: Hosted VS2017
  demands: Cmd

steps:
- script: |
    .scripts/restore.cmd
    .scripts/build.cmd
    .scripts/test.cmd
    .scripts/package.cmd
  env:
    Tag: +$(Build.SourceVersion)
  failOnStderr: true