<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="12.0" DefaultTargets="Build" 
    xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <PropertyGroup>
        <!-- TODO: Make this come from one source of truth for this value -->
        <VowpalWabbitAssemblyVersion Condition="'$(VowpalWabbitAssemblyVersion)' == ''">8.6.1.0</VowpalWabbitAssemblyVersion>
    </PropertyGroup>
</Project>
